const DEFAULT_GIT_API = 'https://api.github.com';
const DEFAULT_GIT_SEARCH_API = `${DEFAULT_GIT_API}/search`;

export const URL_CONSTANTS = {
	GIT_SEARCH: {
		REPOSITORIES: `${DEFAULT_GIT_SEARCH_API}/repositories`,
	}
}
